# Web-AR
Augmented reality 

## Run

Open `index.html` in a browser (or serve the folder with a static server) and allow camera access.

> Note: camera access for Web AR requires a secure context (HTTPS) in most browsers, except `http://localhost` for local development.
